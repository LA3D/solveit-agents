[project]
name = "solveit-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=64.0", "nbdev"]
build-backend = "setuptools.build_meta"

[tool.nbdev]
lib_path = "solveit_agents"
